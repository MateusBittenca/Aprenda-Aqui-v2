# Base do monorepo — comandos de build/start ficam em railway.web.toml / railway.api.toml
[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]
